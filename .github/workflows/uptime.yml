name: Uptime

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check production landing
        run: curl -sfI --max-time 20 https://dashboard.vvbb.fr/fr
